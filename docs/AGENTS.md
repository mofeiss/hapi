# AGENTS.md

Scope: `docs/`.

This checkout currently has a minimal `docs/` workspace plus trader history notes.

## Working rules

- Check actual files in `docs/` before assuming a full docs-site tree exists locally.
- Keep documentation aligned with the current product surface: active agents are Claude and Codex.
- `docs/trader-workspace-history.md` is authoritative on this branch for trader workspace evolution.
- When trader product shape changes, append to the history doc in the same change set.
- If the user asks about `trader-workspace-history`, trader workspace direction, `Desk`, `Overview`, `Accounts`, or historical product rationale, read `docs/trader-workspace-history.md` before answering.
